<h1 align="center">Laporan Praktikum Modul X <br> Sistem Operasi Xinu</h1>
<p align="center">Ardhian Dwi Saputra - 2311104040</p>

---

## Dasar Teori

Xinu (Xinu Is Not Unix) adalah sistem operasi kecil yang digunakan untuk tujuan pembelajaran, terutama untuk memahami konsep dasar sistem operasi seperti manajemen proses, manajemen memori, sistem file sederhana, dan jaringan komputer. Xinu dirancang agar sederhana namun tetap memiliki fitur utama dari sebuah sistem operasi sehingga cocok digunakan sebagai media praktikum.

Xinu menyediakan sebuah shell yang berfungsi sebagai antarmuka antara pengguna dengan sistem operasi. Melalui shell ini, pengguna dapat menjalankan berbagai perintah untuk melihat informasi sistem, mengatur proses, melihat penggunaan memori, serta melakukan konfigurasi jaringan. Dengan adanya shell, pengguna dapat berinteraksi langsung dengan kernel menggunakan perintah-perintah tertentu.

Perintah-perintah pada Xinu digunakan untuk berbagai fungsi seperti menampilkan daftar proses yang sedang berjalan, melihat IP address, melakukan ping ke server lain, membersihkan layar terminal, dan menampilkan statistik memori. Dengan mempelajari dan menjalankan perintah-perintah tersebut, praktikan dapat memahami bagaimana sistem operasi bekerja dalam mengelola sumber daya komputer.

---

## Guided Praktikum

### 1. Menjalankan Xinu
Langkah-langkah:
1. Jalankan Xinu pada emulator/Virtual Machine.
2. Setelah booting selesai, akan muncul tampilan shell Xinu.
3. Shell siap menerima perintah.

### 2. Menampilkan Daftar Perintah
Gunakan perintah:
Fungsi: Menampilkan semua perintah yang tersedia pada Xinu.

### 3. Perintah-perintah pada Xinu dan Fungsinya
Contoh:
- `help` → Menampilkan daftar perintah
- `ps` → Menampilkan daftar proses yang sedang berjalan
- `memstat` → Menampilkan statistik penggunaan memori
- `clear` → Membersihkan layar terminal
- `ipaddr` → Menampilkan IP address
- `ping` → Mengirim paket ping ke host lain
- `kill` → Menghentikan proses
- `exit` → Keluar dari shell
- `sleep` → Menunda proses beberapa detik
- `uptime` → Menampilkan lama sistem berjalan



---

## Hasil dan Pembahasan

Jawaban pertanyaan:

a. Jumlah perintah pada Xinu: (isi sesuai hasil `help`)  
b. 2 perintah yang mempunyai fungsi sama: (contoh: clear dan cls jika ada)  
c. IP address Xinu: (lihat dari perintah `ipaddr`)  
d. Perintah untuk mengetahui IP address: `ipaddr`  
e. IP DNS server yang digunakan oleh Xinu: (lihat dari konfigurasi jaringan)  
f. Jumlah proses yang sedang berjalan: (lihat dari perintah `ps`)  
g. Proses yang mempunyai prioritas paling rendah: (lihat di `ps`)  
h. Proses yang mempunyai ukuran paling besar: (lihat di `ps`)  
i. Proses yang berada dalam state current: (lihat di `ps`)  
j. Proses yang berada dalam state suspend: (lihat di `ps`)  
k. PID dari Main process: (lihat di `ps`)  
l. PPID dari Main process: (lihat di `ps`)  
m. Terminasi proses ps: `kill <PID>`  
n. Terminasi proses shell: `exit` atau `kill <PID shell>`  
o. Lama Xinu sudah berjalan: `uptime`  
p. Perintah untuk membersihkan terminal: `clear`  
q. Perintah untuk menampilkan statistik memori: `memstat`  
r. Ping ke DNS server: `ping <IP DNS>`  
s. Menampilkan "HELLO WORLD!":  
t. Keluar dari terminal Xinu:

---

## Screenshot
Tambahkan screenshot hasil menjalankan perintah di Xinu:
<p align="center">
  <img src="img/help.png" width="500">
</p>

---

## Kesimpulan

Dari praktikum yang telah dilakukan, dapat disimpulkan bahwa Xinu merupakan sistem operasi sederhana yang dapat digunakan untuk mempelajari konsep dasar sistem operasi seperti manajemen proses, memori, dan jaringan. Dengan menggunakan perintah-perintah pada shell Xinu, pengguna dapat melihat dan mengelola sumber daya sistem secara langsung.

---

## Referensi

1. https://en.wikipedia.org/wiki/Data_structure (diakses pada 2026)  
2. https://en.wikipedia.org/wiki/Xinu  
3. Modul Praktikum Sistem Operasi
